DEBUG=False

# Secret key for cryptographic operations (use a secure random string in production)
SECRET_KEY=""

# JWT configuration
JWT_ALGORITHM=""
ACCESS_TOKEN_EXPIRE_MINUTES=5

# Allowed hosts (example: localhost or domain names)
ALLOWED_HOSTS=["localhost", "example.com"]

# Time zone setting
TIME_ZONE="UTC"

# Database configuration
DATABASE__ENGINE="postgresql+psycopg2"
DATABASE__NAME="example_db"
DATABASE__USER="db_user"
DATABASE__PASSWORD="password123"
DATABASE__HOST="localhost"
DATABASE__PORT=5432
